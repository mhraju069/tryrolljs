import { PropsWithChildren } from 'react';
import type { ButtonProps } from './types';
export declare const Button: ({ title, children, onPress, style, touchableOpacityStyle, variant, inverted, disabled, }: PropsWithChildren<ButtonProps>) => JSX.Element;
