{
  "name": "@tryrolljs/root",
  "description": "Entrypoint for Roll monorepo packages",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "yarn workspace @tryrolljs/design-system build",
    "format": "./packages/cli/bin/roll.js format",
    "lint": "./packages/cli/bin/roll.js lint",
    "lint:fix": "./packages/cli/bin/roll.js lint --fix",
    "prerelease": "yarn build",
    "release": "changeset publish"
  },
  "workspaces": [
    "packages/**"
  ],
  "dependencies": {
    "@changesets/changelog-github": "^0.4.6",
    "@changesets/cli": "^2.24.3"
  }
}
